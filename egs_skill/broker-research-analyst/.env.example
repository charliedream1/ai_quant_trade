# 东方财富研报接口（公开，无需 Key）
# 如需启用妙想 API 结构化复核，可配置 EASTMONEY_APIKEY
# EASTMONEY_APIKEY=your_api_key_here

# 缓存目录（相对路径，默认 ./cache）
BRA_CACHE_DIR=./cache

# 默认拉取研报时间窗口（天）
BRA_DEFAULT_DAYS=90

# 研报过期阈值（天），超过则标记为时效性不足
BRA_EXPIRE_DAYS=90

# 请求间隔（秒），控制频率避免被封
BRA_REQUEST_INTERVAL=1.0
