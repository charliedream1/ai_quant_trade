# egs_data 总依赖（所有样例直接依赖的并集，版本已锁定为精确版本 ==）
# 测试环境：conda 环境 py312_quant，Python 3.12.13
# 安装：pip install -r requirements.txt
# 说明：各子样例目录下另有独立的 requirements.txt，仅包含该样例所需依赖。

# 股票类
akshare==1.18.64
baostock==0.9.2
efinance==0.5.8
pyqlib==0.9.7
yfinance==1.4.1
qstock==1.3.8
tushare==1.4.29

# 基金类
# (akshare / efinance 已在上方列出)

# 电子币类
ccxt==4.5.59

# 新闻类
newsapi-python==0.2.7
gdeltdoc==1.12.0
finnhub-python==2.4.28

# 期货类
# (akshare 已在上方列出)

# 宏观经济类
fredapi==0.5.2
wbdata==1.1.0

# 通用基础库
pandas==2.3.3
requests==2.34.2
pandas-datareader==0.10.0
tqdm==4.68.3

# 注意：股票/wind 样例依赖 Wind 终端（WindPy），无法通过 pip 安装，需单独安装万得客户端。
